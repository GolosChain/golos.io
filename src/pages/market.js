import React, { PureComponent } from 'react';
import styled from 'styled-components';

const Wrapper = styled.section``;

export default class Market extends PureComponent {
  render() {
    return <Wrapper>Here should be the market</Wrapper>;
  }
}
